import { describe, it, expect, beforeEach } from 'vitest';
import { {{name_camel}}Service } from '@server/services/{{name_plural}}';

describe('{{name_pascal}}Service', () => {
  beforeEach(() => {
    // Setup test data or mocks
  });

  describe('findAll', () => {
    it('should return all {{name_plural}}', async () => {
      // Arrange
      
      // Act
      const result = await {{name_camel}}Service.findAll();
      
      // Assert
      expect(Array.isArray(result)).toBe(true);
    });
  });

  describe('findById', () => {
    it('should return {{name}} by id', async () => {
      // Arrange
      const id = 'test-id';
      
      // Act
      const result = await {{name_camel}}Service.findById(id);
      
      // Assert
      expect(result).toBeDefined();
    });

    it('should return null for non-existent id', async () => {
      // Arrange
      const id = 'non-existent';
      
      // Act
      const result = await {{name_camel}}Service.findById(id);
      
      // Assert
      expect(result).toBeNull();
    });
  });

  describe('create', () => {
    it('should create a new {{name}}', async () => {
      // Arrange
      const data = {
        name: 'Test {{name_pascal}}',
        description: 'Test description',
      };
      
      // Act
      const result = await {{name_camel}}Service.create(data);
      
      // Assert
      expect(result).toMatchObject(data);
      expect(result.id).toBeDefined();
    });
  });

  describe('update', () => {
    it('should update existing {{name}}', async () => {
      // Arrange
      const id = 'test-id';
      const data = { name: 'Updated Name' };
      
      // Act
      const result = await {{name_camel}}Service.update(id, data);
      
      // Assert
      expect(result?.name).toBe(data.name);
    });
  });

  describe('delete', () => {
    it('should delete {{name}} by id', async () => {
      // Arrange
      const id = 'test-id';
      
      // Act
      const result = await {{name_camel}}Service.delete(id);
      
      // Assert
      expect(result).toBeDefined();
    });
  });
});